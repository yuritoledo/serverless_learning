{
  "name": "serverless_learning",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@vercel/node": "^1.8.1",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@prisma/client": "^2.6.2"
  },
  "scripts": {
    "prisma:migrate": "prisma migrate save --experimental && prisma migrate up --experimental",
    "prisma:generate": "prisma generate",
    "prisma:introspect": "prisma introspect",
    "postinstall": "prisma generate"
  }
}